name: ir2
channels:
  - defaults
dependencies:
  - python=3.7
  - numpy-base
  - numpy
  - torchvision
  - cudatoolkit=10.2
  - pytorch
  - matplotlib
  - pip:
    - sentence-transformers
prefix: /home/stefan/anaconda3/envs/ir2
