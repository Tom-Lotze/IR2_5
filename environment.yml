name: ir2
channels:
  - defaults
dependencies:
  - python=3.7
  - numpy-base
  - numpy
  - pip
  - cudnn
  - cudatoolkit=10.2
  - pytorch
  - torchvision
  - matplotlib
  - pip:
    - sentence-transformers
prefix: /home/stefan/anaconda3/envs/ir2
